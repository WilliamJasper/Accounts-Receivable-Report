# คัดลอกไฟล์นี้เป็น .env แล้วแก้ค่าให้ตรงกับเครื่องของคุณ (ไฟล์ .env จะไม่ถูก commit)
# SQL Server - RegisterDB_2569
# ใช้ Trusted Connection (Windows) หรือใส่ User/Password ตามที่ใช้
DATABASE_URL=

# OMODA - สำหรับหน้าตรวจสอบสถานะตั้งเบิก (Claim Report)

OMODA_DATABASE_URL=

